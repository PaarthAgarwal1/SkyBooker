server:
  port: 8085

spring:
  application:
    name: PASSENGER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/skyBooker
    username: root
    password: Paarth@29122003
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: my-super-secret-key-my-super-secret-key-12345